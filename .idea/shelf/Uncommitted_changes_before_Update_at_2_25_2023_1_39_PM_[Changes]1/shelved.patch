Index: pages/api/user/[userId].js
===================================================================
diff --git a/pages/api/user/[userId].js b/pages/api/user/[userId].js
deleted file mode 100644
--- a/pages/api/user/[userId].js	
+++ /dev/null	
@@ -1,61 +0,0 @@
-import clientPromise from "@utils/database/mongodb";
-import {throwError} from '@common_imports'
-
-export default async function handler(req, res) {
-  const { userId } = req.query
-  const client = await clientPromise;
-  const db = client.db(process.env.DB_NAME);
-  const bodyObject = {userId: userId}
-  switch (req.method) {
-    case "POST":
-      try {
-        let findResponse = await db.collection("users").findOne(bodyObject)
-        
-        if(findResponse) {
-          res.json({
-            code: 400,
-            message: "User already in database.",
-            data: {},
-          });
-        } else {
-          await db.collection("users").insertOne(bodyObject);
-
-          res.json({
-            code: 200,
-            message: "Created user.",
-            data: {},
-          });
-        }
-      } catch (error) {
-        throwError("Could not create user. " + error)
-      }
-      return;
-    case "GET":
-      let findResponse = await db.collection("users").findOne(bodyObject)
-        
-      if(findResponse) {
-        res.json({
-          code: 200,
-          message: "Loging into user " + findResponse._id + ".",
-          data: findResponse,
-        });
-      } else {
-        res.json({
-          code: 400,
-          message: "User not in database.",
-          data: {},
-        });
-      }
-      return;
-  }
-}
-
-function userGet(params) {
-  
-}
-function userPut(params) {
-  
-}
-function userPost(params) {
-  
-}
\ No newline at end of file
Index: pages/stopwatch.js
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/pages/stopwatch.js b/pages/stopwatch.js
new file mode 100644
--- /dev/null	
+++ b/pages/stopwatch.js	
@@ -0,0 +1,14 @@
+import React from 'react'
+import { useStopWatch } from '@hooks/useStopWatch'
+export default function Stopwatch() {
+    const {start,stop,reset,isRunning,time} = useStopWatch()
+  return (
+    <>
+    <div>stopwatch</div>
+    <p>{time}</p>
+    <button onClick={()=>start()}>start</button>
+    <button onClick={()=>stop()}>stop</button>
+
+    </>
+  )
+}
Index: pages/api/petId.js
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/pages/api/petId.js b/pages/api/petId.js
--- a/pages/api/petId.js	
+++ b/pages/api/petId.js	
@@ -8,12 +8,12 @@
     switch (req.method) {
         case "POST":
             // create a filter for a movie to update
-            const filter = { userId: bodyObject.userId };
+            const filter = { username: bodyObject.username };
             
             // create a document that sets the plot of the movie
             const updateDoc = {
             $set: {
-                petId: bodyObject.petId
+                pet_id: bodyObject.petId
             },
             };
             try{
Index: pages/api/avatarId.js
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/pages/api/avatarId.js b/pages/api/avatarId.js
--- a/pages/api/avatarId.js	
+++ b/pages/api/avatarId.js	
@@ -7,13 +7,11 @@
     const bodyObject = req.body;
     switch (req.method) {
         case "POST":
-            // create a filter for a movie to update
-            const filter = { userId: bodyObject.userId };
-            
-            // create a document that sets the plot of the movie
+            const filter = { username: bodyObject.username };
+
             const updateDoc = {
             $set: {
-                avatarId: bodyObject.avatarId
+                avatar_id: bodyObject.avatarId
             },
             };
             try{
